# Bumblebee GUI — Handoff Document

**Date:** 2026-05-28  
**Session:** Pi (MiMo v2.5 Pro)  
**Status:** Phase 1 (P0 Bug Fixes) complete, Phase 3 (UI Redesign) pending

---

## Current State

### What's Done

| Phase | Status | Commits |
|-------|--------|---------|
| 1. P0 Bug Fixes | ✅ Complete | 6 commits |
| 2. Playwright Setup | ⏸️ Deferred | 1 commit |
| 3. UI Redesign | ⏳ Pending | — |
| 4. Exposure Catalogs | ⏳ Pending | — |
| 5. Architecture | ⏳ Pending | — |
| 6. Polish | ⏳ Pending | — |

### Git State

- **Branch:** `refactor/v2` (pushed to GitHub)
- **Remote:** `https://github.com/ruangraung/bumblebee-gui.git`
- **Commits:** 9 total on refactor/v2
- **Working tree:** Clean

### Key Files

| File | Purpose |
|------|---------|
| `docs/DESIGN.md` | Visual spec (colors, typography, components) |
| `docs/2026-05-28-refactor-plan.md` | Bug analysis from Nadya |
| `docs/2026-05-28-implementation-plan.md` | Execution plan |
| `docs/handoff-bumblebee-gui.md` | Original handoff (session 1) |
| `docs/handoff-2026-05-28.md` | This document (session 2) |

---

## What Was Fixed (P0 Bugs)

### Backend

| File | Fix |
|------|-----|
| `backend/bumblebee_gui/models.py` | PackageRecord + FindingRecord fields corrected |
| `backend/bumblebee_gui/scanner.py` | NDJSON field mapping fixed (record_type, package_name, etc.) |

### Frontend

| File | Fix |
|------|-----|
| `frontend/src/lib/api.ts` | TypeScript types aligned with backend |
| `frontend/src/pages/Scan.tsx` | Ecosystem list corrected (removed cargo, maven, nuget, cocoapods) |
| `frontend/src/pages/Findings.tsx` | Field references fixed (found_in → source_file) |
| `frontend/src/pages/Results.tsx` | Columns + CSV export updated |

### Infrastructure

| File | Change |
|------|--------|
| `docker-compose.yml` | Ports bound to 127.0.0.1 (localhost only) |
| `Dockerfile.frontend` | Playwright setup added (deferred) |
| `.dockerignore` | Exclude node_modules from build |
| `.gitignore` | Exclude .codegraph directory |

---

## What's Next (Phase 3: UI Redesign)

### Goal
Match the visual spec in `docs/DESIGN.md`.

### Tasks

1. **Update CSS variables** (`frontend/src/index.css`)
   - Replace current variables with DESIGN.md tokens
   - Light mode + dark mode colors

2. **Update Tailwind config** (`frontend/tailwind.config.js`)
   - Add ecosystem colors (npm, pypi, go, rubygems, packagist)
   - Add severity colors (critical, high, medium, low, info)

3. **Create Badge components** (`frontend/src/components/ui/badge.tsx`)
   - EcosystemBadge: color-coded by ecosystem
   - SeverityBadge: color-coded by severity

4. **Update page layouts**
   - Dashboard: 2-col mobile, 3-col desktop grid
   - Scan: Collapsible sections with proper styling
   - Results: Table with new columns, badge rendering
   - Findings: Severity left-border accent
   - Settings: Label-caps section headers

### Reference

- **DESIGN.md** is the source of truth for all visual decisions
- **Sub-agents can be used** for parallel page updates
- **No Docker rebuilds needed** — just file editing

---

## Important Context

### Security

- **Docker ports:** Bound to 127.0.0.1 (localhost only)
- **Gitleaks:** Pre-commit hook active (`.git/hooks/pre-commit`)
- **No npm on host:** All npm operations inside Docker
- **No PyPI:** Distribution via GitHub Releases only

### User Preferences

- **Mac user:** Apple Silicon, uses OrbStack for Docker
- **Security-conscious:** No plaintext credentials, no shell history
- **Workflow:** Prefers sub-agents for parallel work
- **Testing:** Playwright deferred due to VPS resource constraints

### Known Issues

- **Docker node_modules:** Volume mount was stale, removed in docker-compose.yml
- **Playwright:** Not installed in container (deferred)
- **SSE streaming:** Not implemented yet (Phase 5)

---

## Commands for Next Session

### Resume Work

```bash
cd ~/pi-workspace/projects/bumblebee-gui
git checkout refactor/v2
git pull
```

### Start Docker (When Needed)

```bash
docker compose up -d
```

### Check Git Status

```bash
git status
git log --oneline -10
```

---

## Architecture Summary

```
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│   Browser UI    │────▶│  Python API     │────▶│   Bumblebee     │
│   (React/TS)    │◀────│  (FastAPI)      │◀────│   CLI Binary    │
└─────────────────┘     └─────────────────┘     └─────────────────┘
     Port 5173              Port 8001              Subprocess
```

### Tech Stack

| Component | Technology |
|-----------|------------|
| Frontend | React 18 + TypeScript + TailwindCSS + shadcn/ui |
| Backend | Python 3.11 + FastAPI + SQLite |
| Scanner | Bumblebee CLI v0.1.1 (Go binary) |
| Dev Environment | Docker Compose |

---

## Key Decisions Made

1. **Branch strategy:** Feature branch `refactor/v2` (not main)
2. **UI spec:** DESIGN.md from Nadya is source of truth
3. **Playwright:** Deferred due to VPS resources
4. **Docker ports:** Localhost only (127.0.0.1)
5. **Distribution:** GitHub Releases (no PyPI)

---

## Resume Instructions

When returning to this project:

1. Read this handoff document
2. Read `docs/DESIGN.md` for visual spec
3. Read `docs/2026-05-28-implementation-plan.md` for execution plan
4. Check git status and branch
5. Continue with Phase 3 (UI Redesign)

---

**Created:** 2026-05-28  
**Purpose:** Handoff for next session  
**Previous handoff:** `docs/handoff-bumblebee-gui.md` (session 1)
